import React from 'react';

/*Context for providing authentication related functionality (e.g. login and
  signup) and information (e.g. currently logged in user).*/
const AuthenticationContext = React.createContext(null);

export default AuthenticationContext;

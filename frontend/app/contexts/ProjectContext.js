import React from 'react';

/*Context for providing project related functionality (e.g. creation)
  and information (e.g. list of projects).*/
const ProjectContext = React.createContext(null);

export default ProjectContext;
